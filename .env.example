# Set per-deploy. Stripe creds live in AWS Secrets Manager in real envs;
# this file exists so local tooling (Stripe CLI, smoke tests) can authenticate.

STRIPE_SECRET_KEY=sk_test_replace_me
STRIPE_WEBHOOK_SECRET=whsec_replace_me

# Default bedrock model for the example workflow.
BEDROCK_MODEL_ID=anthropic.claude-3-5-sonnet-20241022-v2:0
